groups:
  - name: master
    jobs:
      - development
      - management-dev
  - name: pull-request
    jobs:
      - dataworks-development-tools-pr
  - name: update-pipeline
    jobs:
      - update-pipeline
